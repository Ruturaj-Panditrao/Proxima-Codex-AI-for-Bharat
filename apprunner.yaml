version: 1.0
runtime: python311

build:
  commands:
    build:
      - python3 -m pip install -r stream/requirements.txt

run:
  command: sh -c 'python3 -m pip install -r stream/requirements.txt && python3 -m uvicorn stream.app:app --host 0.0.0.0 --port 8080'
  network:
    port: 8080